  <script>
    {{-- $(function() {
      console.log('ok')
      $('.submit_btn').on('click', function(e) {
        e.preventDefault()
        var form = $(this).closest('form')
        var url = form.attr('action');
        var inputs = form.find('input[type="text"]')
        for (i = 0; i < inputs.length; i++) {
          url = url + '/' + $(inputs[i]).val()
        }
        location = url
      })
    }) --}}

  </script>
